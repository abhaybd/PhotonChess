[project]
name = "sprt"
version = "0.1.0"
description = "SPRT utility for Photon"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "GitPython",
    "chess",
    "tqdm",
]

[project.optional-dependencies]
elo = [
    "hydra-core>=1.3",
    "maia3 @ git+https://github.com/CSSLab/maia3.git",
]
